{
  "name": "base-post-booster",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "vercel-build": "npm install && npx prisma generate && next build"
  },
  "dependencies": {
    "next": "14.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@prisma/client": "^5.5.0"
  },
  "devDependencies": {
    "prisma": "^5.5.0"
  }
}
